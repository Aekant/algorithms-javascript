/**
 * A linked list is made of nodes pointing to the following node in the list (next)
 * A Doubly linked list has nodes that are pointing to two nodes. One is the following node (next) and the other is
 * preceeding node (prev)
 * 
 * In addition to that either of the linked lists have a head and tail node
 */

/**
 * So here in this file there is a LinkedList class with a bunch of methods in it
 */

export class LinkedList {
  
}